name: Deploy

on:
  push:
    branches: [main]
    paths-ignore:
      - 'docs/**'
      - 'scripts/**'
      - 'workflows/build.yml'
  pull_request:
    branches: [main]
    paths-ignore:
      - 'docs/**'
      - 'scripts/**'
      - 'workflows/build.yml'
  release:
    types: [created]

jobs:
  build_wheel:
    name: Build Wheel
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v3
        with:
          python-version: '3.9'

      - name: Install build dependencies
        run: |
          python -m pip install --upgrade pip
          pip install build

      - name: Build wheel
        run: |
          python -m build --wheel

      - uses: actions/upload-artifact@v4
        with:
          path: dist/*.whl
          name: artifacts-wheel

  build_sdist:
    name: Build Source Distribution
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v3
        with:
          python-version: '3.9'

      - name: Install build dependencies
        run: |
          python -m pip install --upgrade pip
          pip install build

      - name: Build sdist
        run: |
          python -m build --sdist

      - uses: actions/upload-artifact@v4
        with:
          path: dist/*.tar.gz
          name: artifacts-sdist

  publish-to-testpypi:
    name: Publish to Test PyPI
    needs: [build_wheel, build_sdist]
    runs-on: ubuntu-latest
    if: github.event_name == 'release' && success()

    environment:
      name: testpypi
      url: https://test.pypi.org/p/omnipose_ci # Change omnipose_ci to omnipose

    permissions:
      id-token: write  # IMPORTANT: mandatory for trusted publishing

    steps:
    - name: Download artifacts
      uses: actions/download-artifact@v4
      with: 
        name: artifacts-*
        path: dist
        merge: true
    - name: Download all the dists
      uses: actions/download-artifact@v4
      with:
        name: python-package-distributions
        path: dist/
    - name: Publish distribution to TestPyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          repository-url: https://test.pypi.org/legacy/

  # UNCOMMENT FOR UPLOAD TO PYPI
  # Link to the documentation: https://packaging.python.org/en/latest/guides/publishing-package-distribution-releases-using-github-actions-ci-cd-workflows/
  # publish-to-pypi:
  #   name: Publish to PyPI
  #   needs: [build_wheel, build_sdist]
  #   runs-on: ubuntu-latest
  #   if: github.event_name == 'release' && success()

  #   environment:
  #     name: pypi
  #     url: https://pypi.org/p/omnipose_ci # Change omnipose_ci to omnipose
  #   permissions:
  #     id-token: write  # IMPORTANT: mandatory for trusted publishing

  #   steps:
  #   - name: Download artifacts
  #     uses: actions/download-artifact@v4
  #     with: 
  #       name: artifacts-*
  #       path: dist
  #       merge: true
  #   - name: Download all the dists
  #     uses: actions/download-artifact@v4
  #     with:
  #       name: python-package-distributions
  #       path: dist/
  #   - name: Publish distribution 📦 to PyPI
  #     uses: pypa/gh-action-pypi-publish@release/v1